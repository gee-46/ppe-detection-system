# Core ML / CV
ultralytics>=8.4.0
torch>=2.10.0
torchvision>=0.25.0

# API server used for /predict/image and /predict/video (headless: no GUI
# deps needed for server-side inference/Docker). If you want to run the
# local interactive demo scripts in app/video_detection.py (cv2.imshow
# windows), install `opencv-python` instead of/alongside this on your
# machine — headless and non-headless opencv conflict if both are
# installed, so only add opencv-python where you actually need a GUI window.
opencv-python-headless>=4.10.0

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
pydantic>=2.9.0
pyyaml>=6.0

# Numerical
numpy>=1.26.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
